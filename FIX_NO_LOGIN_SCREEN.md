# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Нет окна авторизации

**Дата:** 2025-11-27  
**Проблема:** При запуске приложения нет экрана входа

---

## 🔍 Диагностика

### Проблема 1: Backend уже запущен ✅
```
Backend работает на порту 4000 (PID 15372)
Это НОРМАЛЬНО - не трогайте!
```

### Проблема 2: Нет окна авторизации ❌

**Причина:** В localStorage сохранен старый токен или dev-пользователь

---

## ✅ РЕШЕНИЕ

### Вариант 1: Очистить localStorage (РЕКОМЕНДУЕТСЯ)

1. Откройте браузер
2. Нажмите **F12** (открыть DevTools)
3. Перейдите на вкладку **Application** (или **Хранилище**)
4. В левом меню найдите **Local Storage**
5. Выберите `http://localhost:3000` или `http://localhost:4173`
6. Нажмите **Clear All** (Очистить все)
7. Обновите страницу (**F5**)

**Скриншот:**
```
Application
├── Local Storage
│   └── http://localhost:3000
│       ├── __auth_token__  ← Удалить
│       └── __current_user__ ← Удалить
```

### Вариант 2: Через консоль браузера

1. Откройте браузер
2. Нажмите **F12**
3. Перейдите на вкладку **Console**
4. Выполните:
```javascript
localStorage.clear();
location.reload();
```

### Вариант 3: Incognito режим

1. Откройте браузер в режиме **Инкогнито** (Ctrl+Shift+N)
2. Перейдите на http://localhost:3000/_PL-tests/
3. Должен появиться экран входа

---

## 🎯 После очистки

### В DEV режиме (`npm run dev`):
- ✅ Автоматический вход как "Admin (DEV)"
- ✅ Переключатель ролей в левом нижнем углу
- ✅ Не требуется backend

### В PREVIEW режиме (`npm run preview`):
- ✅ Экран входа
- ✅ Нужно ввести email/пароль
- ✅ Требуется backend на порту 4000

---

## 📋 Правильная последовательность запуска

### Для DEV (без авторизации):
```bash
npm run dev
```
- Откроется: http://localhost:3000/_PL-tests/
- Автоматический вход как admin

### Для PREVIEW (с авторизацией):

**Терминал 1:**
```bash
cd backend
npm run dev
```

**Терминал 2:**
```bash
npm run build
npm run preview
```

**Браузер:**
1. Откройте: http://localhost:4173/_PL-tests/
2. **Очистите localStorage** (F12 → Application → Clear All)
3. Обновите страницу (F5)
4. Должен появиться экран входа
5. Войдите: admin@example.com / Admin123!

---

## 🔧 Если проблема осталась

### Проверьте консоль браузера (F12 → Console)

**Не должно быть ошибок:**
- ❌ `ERR_CONNECTION_REFUSED` - backend не запущен
- ❌ `401 Unauthorized` - неверный токен
- ❌ `Cannot read property of null` - проблема с кодом

**Должно быть:**
- ✅ Успешные запросы к `/api/auth/me` или `/api/auth/login`
- ✅ Нет ошибок в консоли

### Проверьте Network tab (F12 → Network)

**При открытии приложения:**
1. Если есть токен в localStorage:
   - Запрос: `GET /api/auth/me`
   - Ответ: `200 OK` с данными пользователя
   
2. Если нет токена:
   - Должен показаться экран входа
   - После ввода данных: `POST /api/auth/login`
   - Ответ: `200 OK` с токеном

---

## 🎉 Ожидаемый результат

### DEV режим:
```
┌────────────────────────────────────┐
│  Путевые листы                     │
├────────────────────────────────────┤
│  Панель управления                 │
│  Путевые листы                     │
│  ...                               │
│                                    │
│  Роль: admin ▼  прав: 22          │ ← Переключатель
└────────────────────────────────────┘
```

### PREVIEW режим (после очистки localStorage):
```
┌────────────────────────────────────┐
│  🚚 Путевые листы                  │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ Email:                       │ │
│  │ [admin@example.com        ]  │ │
│  │                              │ │
│  │ Пароль:                      │ │
│  │ [**********              ]   │ │
│  │                              │ │
│  │        [Войти]               │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
```

---

## 📞 Если ничего не помогло

1. **Полностью закройте браузер** и откройте заново
2. **Попробуйте другой браузер** (Chrome, Firefox, Edge)
3. **Проверьте, что backend работает:**
   ```bash
   curl http://localhost:4000/api/auth/me
   # Должно вернуть: {"success":false,"error":{"message":"..."}}
   ```

---

**Подготовлено:** Antigravity AI Assistant  
**Дата:** 2025-11-27
