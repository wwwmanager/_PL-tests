# ⚡ БЫСТРОЕ РЕШЕНИЕ: Настройка Windows для работы с Prisma

## 🔴 Проблема
Windows Firewall блокирует Node.js при скачивании Prisma engines из-за того, что сеть определена как "Общедоступная" (Public).

**Ошибка:**
```
Error: Client network socket disconnected before secure TLS connection was established
```

---

## ✅ Решение (выберите ОДИН вариант)

### Вариант 1: Изменить тип сети на "Частная" (РЕКОМЕНДУЕТСЯ)

#### Способ A: Через GUI (самый простой)

1. Нажмите **Win + I** (откроется Параметры Windows)
2. **Сеть и Интернет** → **Ethernet** (или **Wi-Fi**)
3. Кликните на активное подключение **"Сеть"**
4. В разделе **"Тип сетевого профиля"** выберите **"Частный"**
5. Закройте окно
6. Вернитесь в PowerShell и выполните: `npx prisma generate`

#### Способ B: Через PowerShell (от администратора)

1. **Правой кнопкой по PowerShell** в меню Пуск → **Запуск от имени администратора**
2. Выполните:
   ```powershell
   cd C:\_PL-tests\backend
   .\change-network-to-private.ps1
   ```
3. Закройте окно администратора
4. В обычном PowerShell: `npx prisma generate`

---

### Вариант 2: Добавить Node.js в исключения брандмауэра

Если не хотите менять тип сети:

1. **Правой кнопкой по PowerShell** в меню Пуск → **Запуск от имени администратора**
2. Выполните:
   ```powershell
   cd C:\_PL-tests\backend
   .\add-node-to-firewall.ps1
   ```
3. Закройте окно администратора
4. В обычном PowerShell: `npx prisma generate`

---

## 📝 После успешного generate

Когда `npx prisma generate` завершится успешно (без ошибок), продолжайте setup:

```powershell
cd C:\_PL-tests\backend

# 1. Создать миграцию БД (когда спросит имя, введите: init)
npm run prisma:migrate

# 2. Заполнить тестовыми данными
npm run prisma:seed

# 3. Запустить backend сервер
npm run dev
```

---

## 🎯 Статус версий

После отката на стабильную версию:
- **Prisma**: 5.22.0 ✅
- **Node.js**: v20.19.5 ✅
- **NPM**: 10.8.2 ✅

Prisma 7.x имеет проблемы с доступностью engines (404 ошибки), поэтому мы откатились на Prisma 5.22.0.

---

## ❓ Если всё ещё не работает

1. Убедитесь, что вы изменили тип сети на "Частная" ИЛИ добавили Node.js в исключения
2. Перезапустите PowerShell
3. Попробуйте: `npx prisma generate`

Если проблема сохраняется — напишите текст ошибки!
