# AI Assistant Instructions

## 📋 Quick Reference

This file serves as the **entry point** for AI assistants working on this project.

### 🎯 Project Overview
- **Name:** Waybill Management Application
- **Stack:** React 19 + TypeScript + Vite / Node.js + Express + TypeORM + PostgreSQL
- **Modes:** Central (backend API) + Driver (offline, IndexedDB)

### 📂 Key Files for AI Context

1. **`Tasks/APPLICATION_CONTEXT.md`** — Complete project context, architecture, rules
2. **`Tasks/implementation_plan.md`** — Current implementation plan and roadmap
3. **`Tasks/task.md`** — Active task checklist and objectives
4. **`.agent/workflows/performance-optimization.md`** — AI behavior rules
5. **`.agent/context/session-startup.md`** — Detailed session startup algorithm

---

## 🚀 AI Session Startup Protocol

**On EVERY new session, the AI MUST:**

1. **Read context files** (in order):
   - `Tasks/APPLICATION_CONTEXT.md`
   - `Tasks/implementation_plan.md` (if exists)
   - `Tasks/task.md` (if exists)

2. **Report status** in first response:
   ```
   ✅ Kontekst zagrujen:
   - APPLICATION_CONTEXT.md прочитан
   - implementation_plan.md прочитан
   - task.md прочитан
   
   Текущая задача: [from task.md]
   Следующие шаги: [from plan]
   ```

3. **Auto-use Context7** when:
   - Code generation involves libraries/frameworks
   - Setup/configuration steps needed
   - Library/API documentation required

---

## 🔄 Continuous Maintenance Rules

### When making significant changes:

**Update `APPLICATION_CONTEXT.md`** if changing:
- Architecture or key decisions
- Database schema / ORM / data access layer
- Central/Driver mode approach
- Auth / RBAC / data storage strategy
- Technology stack or major dependencies

**Update `implementation_plan.md`** when:
- Completing plan phases/steps (mark with ✅)
- Discovering new issues or requirements
- Changing approach within current plan

**Update `task.md`** when:
- Starting work on a task (`[ ]` → `[/]`)
- Completing a task (`[/]` → `[x]`)
- Main objective changes or evolves

### Format for updates:
- **Incremental edits** preferred (not full rewrites)
- Add **history entries** to APPLICATION_CONTEXT.md with dates (YYYY-MM-DD)
- Keep markdown **concise and structured**

---

## 📚 Documentation Structure

```
c:\_PL-tests/
├── .ai-instructions.md          ← You are here (quick reference)
├── .agent/
│   ├── workflows/
│   │   └── performance-optimization.md  ← AI rules & workflows
│   ├── context/
│   │   └── session-startup.md           ← Detailed startup algorithm
│   └── templates/
│       ├── implementation_plan.template.md
│       └── task.template.md
└── Tasks/
    ├── APPLICATION_CONTEXT.md    ← Project context (main truth)
    ├── implementation_plan.md    ← Current plan
    └── task.md                   ← Active task checklist
```

---

## 🎯 AI Assistant Principles

1. **Context-First:** Always read and align with context files before acting
2. **Incremental Updates:** Keep documentation in sync with code changes
3. **Explicit Communication:** Report what was loaded, what will be done
4. **Context7 Auto-Usage:** Use documentation tools proactively
5. **Plan Adherence:** Follow existing plans; propose updates if diverging

---

## 💡 Quick Commands

- **Start new feature:** Create/update `implementation_plan.md` first
- **Daily work:** Check `task.md` for current objectives
- **Understand project:** Read `APPLICATION_CONTEXT.md`
- **After major change:** Update history in APPLICATION_CONTEXT.md

---

**Last Updated:** 2025-11-30
